name: Lint Server

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  lint-server:
    name: Lint Server
    runs-on: ubuntu-latest
    strategy:
      matrix:
        go-version: ['1.23']

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: ${{ matrix.go-version }}
        cache: false

    - name: Cache Go modules
      uses: actions/cache@v4
      with:
        path: |
          ~/go/pkg/mod
          ~/.cache/go-build
        key: ${{ runner.os }}-go-${{ matrix.go-version }}-${{ hashFiles('**/go.sum') }}
        restore-keys: |
          ${{ runner.os }}-go-${{ matrix.go-version }}-

    - name: Download dependencies
      run: go mod download

    - name: Install golangci-lint
      run: |
        # Install latest v1.x version (config is NOT compatible with v2)
        go install github.com/golangci/golangci-lint/cmd/golangci-lint@latest
        echo "$(go env GOPATH)/bin" >> $GITHUB_PATH

    - name: Run golangci-lint on server code
      run: golangci-lint run ./internal/mcp/... ./internal/auth/... ./internal/config/... ./internal/crypto/... ./internal/database/... ./internal/resources/... ./internal/tools/... ./cmd/pgedge-pg-mcp-svr/...

    - name: Run go vet on server code
      run: go vet ./internal/mcp/... ./internal/auth/... ./internal/config/... ./internal/crypto/... ./internal/database/... ./internal/resources/... ./internal/tools/... ./cmd/pgedge-pg-mcp-svr/...

    - name: Check formatting
      run: |
        gofmt -l ./internal/mcp ./internal/auth ./internal/config ./internal/crypto ./internal/database ./internal/resources ./internal/tools ./cmd/pgedge-pg-mcp-svr > /tmp/gofmt.out
        if [ -s /tmp/gofmt.out ]; then
          echo "The following files are not formatted:"
          cat /tmp/gofmt.out
          exit 1
        fi
