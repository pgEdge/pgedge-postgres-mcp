# pgEdge MCP Regression Test Configuration Template
#
# Copy this file to .test.env and edit values as needed:
#   cp .test.env.example .test.env
#
# Or run the interactive wizard:
#   make config

# ============================================================================
# EXECUTION MODE
# ============================================================================
# Where to run tests:
#   local            - Run on local machine
#   container-systemd - Run in Docker container with systemd
TEST_EXEC_MODE=container-systemd

# ============================================================================
# SERVER ENVIRONMENT
# ============================================================================
# Which repository to use:
#   live    - Production repository (https://dnf.pgedge.com)
#   staging - Staging repository (https://dnf.pgedge.com/staging)
#
# Note: Staging may be slow or timeout. Use 'live' for reliable tests.
TEST_SERVER_ENV=live

# ============================================================================
# CONTAINER OS IMAGE
# ============================================================================
# Which OS to test (only used when TEST_EXEC_MODE=container-systemd):
#   almalinux:10  - AlmaLinux 10 (RHEL-based, recommended)
#   almalinux:9   - AlmaLinux 9
#   ubuntu:24.04  - Ubuntu 24.04 (Debian-based)
#   debian:12     - Debian 12
#   rocky:9       - Rocky Linux 9
TEST_OS_IMAGE=almalinux:10

# ============================================================================
# POSTGRESQL VERSION
# ============================================================================
# Which PostgreSQL version to install and test:
#   16 - PostgreSQL 16
#   17 - PostgreSQL 17
#   18 - PostgreSQL 18 (recommended)
TEST_PG_VERSION=18

# ============================================================================
# LOGGING LEVEL
# ============================================================================
# How verbose should test output be:
#   minimal  - Show only test summary and failures
#   detailed - Show all test steps and command output
TEST_LOG_LEVEL=minimal

# ============================================================================
# TEST TIMEOUT
# ============================================================================
# Maximum time to allow for full test suite:
#   Format: 30m, 1h, 60m, etc.
#   Recommended: 30m for live, 60m for staging
TEST_TIMEOUT=30m
